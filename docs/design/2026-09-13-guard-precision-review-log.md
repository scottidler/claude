# Chunk B review log: guard-precision

Companion to `2026-09-13-guard-precision.md`. The DECISIONS live in that doc's Resolved Decisions section. This file holds the round-by-round history behind them: who found what, in which round, what was pushed back, and what the fold missed and had to be swept.

It exists because the design doc was reproducing the exact disease it is meant to help fix. Audit item 4 measured review-process inflation (docs growing 302 to 578, 458 to 778); this doc went 378 to 536 across three panel rounds, and its Resolved Decisions section reached 22% of the word count with roughly half of that being minutes rather than rulings. Item 13's prescription is a body under 600 lines with the history in an appendix. This is the appendix.

Nothing here is dropped content: the text below is the Resolved Decisions section verbatim as it stood after round 3 and the intent pass, before it was compressed.

## Panel budget spent

Three rounds, the cap in `rules/interaction.md`. Round 2's staff seat died at its banner and produced nothing; round 2's synthesis arrived after round 3 and still carried two must-fix defects nobody else had found.

## Verbatim history

- 2026-09-13, Fable subagent reading the four audit posts for intent, folded. The doc contradicted itself on the program's own headline rule. Phase 6 deletes the `git -C` paragraph from `rules/git.md` with the reasoning that "a rule telling the model not to type it is prose with nothing behind it", while the 18-line "Branch names: NEVER a slash" section survived untouched even though Phase 5 builds `branch-name-guard.sh` to enforce it, and the risk table cited that prose as a mitigation. Same shape, opposite treatment. Phase 8 now cuts it to a one-line pointer and a criterion proves it. Enforcement before prose has to apply to the rule that is convenient to keep, or it is not a rule.
- 2026-09-13, author, closes OQ1 WITHOUT escalation, because it was never Scott's to decide. The `git commit` deny on `tatari-tv/*` main ships in chunk B. The ranked report places it there itself: item 5's change list reads "allow path-scoped checkout/restore, deny `git tag -f` and off-main tag creation, match `(^|/)(bump|release)-`, deny `git commit` on main in `tatari-tv/*`, mask `$(...)`, expand `$TMPDIR` in `--body-file`". The `incidents` lens splits the same verb into two proposals on purpose: proposal 2 is the release-guard fix list carrying this gate, proposal 8 is the rails commit AUTHORIZATION that needs the last human prompt and belongs to chunk D. So the audit had already decided these are two different guards on one verb, and pushing this one to D would be deferring an item 5 change, which `taste.md` says needs Scott's say-so that nobody asked for.
  My branch-state versus prompt-context rationale was correct but derived, and deriving it was the error: `taste.md` says open questions are the author's to close by probing, and the answer was in evidence already read this session.
  The one soft spot, stated so it is not rediscovered: the OVERVIEW post's item 5 line says only "close the release-guard gaps" and does not enumerate the commit deny, so a reader of that post alone would not find it. The dense report and the incidents lens both enumerate it.
- 2026-09-13, review panel round 3 synthesis, folded, and it is the third time a later phase invalidated an earlier-written acceptance criterion (AC7 in round 1, AC1's seven-vs-eight in round 2, AC1's eight-vs-nine now). Each was a count or a command written from the design rather than from the tree the plan would actually produce. AC1 is rewritten to assert the PROPERTY it means, that these guards source the library, instead of counting files that contain a string; a count is hostage to every later phase, a property is not. The general lesson, recorded once: an acceptance criterion phrased as a census of the final tree has to be re-derived every time the plan changes, and that re-derivation is exactly the step that keeps getting skipped.
- 2026-09-13, recorded: every "Observed on main" line in this doc was re-run independently and confirmed, including the 2a bypass still being live on `main`. The `-C` finding applies to Phase 5 alone: a masked `-C` in Phase 6 means the rewrite does not fire and the command runs unchanged, so nothing is lost there.
- 2026-09-13, review panel round 2 synthesis (arriving after round 3; folded on the same terms), THREE must-fix, two of which are deny-to-allow regressions my own design would have introduced. Both verified 2026-09-13 by feeding the live hooks their JSON:
  - `echo "$(git push origin --tags)"` DENIES today. Under the draft the subshell body was masked and the command word was `echo`, so `cmdword_is git` failed too: ALLOW, while the shell still ran the bulk tag push inside the substitution.
  - `bash -c 'echo $SOME_TOKEN'` DENIES today. Under the draft the single-quoted span was masked: ALLOW, while the child shell expanded the secret.
  Both are the round-1 double-quote defect in two more span classes, which is the signal that the per-case fixes were not converging on a principle. The principle: a masker may only erase a span the shell will never execute. Subshell bodies, backticks and `-c` arguments are nested statements, so `stmts` yields them and neutralizes the span in the enclosing statement, rather than any masker erasing them. `mask_subshell` is deleted. That single rule closes both regressions, keeps the measured `$(git describe --tags)` false positive fixed, and is why the Data Model now states a rule instead of enumerating cases.
- 2026-09-13, review panel round 2 synthesis, folded. The measured-false-positive table attributed the HALL-OF-SHAME heredoc to `git-release-guard.sh`, which already allows it: that guard grew `strip_heredocs` on 2026-09-01. Verified live. The class reproduces against `manifest-scope-guard.sh`, which has no heredoc strip, and the row now says so. A design that cites a false positive against the wrong guard is citing a fix that already shipped.
- 2026-09-13, review panel round 2 synthesis, folded. `git tag -a v1 -m "fix the -d flag"` would read as a tag DELETION once the new tag gates land, because the exclusion list scans for `-d` anywhere in the statement. Verified allow today (no tag-creation gate exists yet). `mask_optarg` already fixes it by hiding the `-m` value; the case is now in the Phase 4 list so the fix is proven rather than assumed.
- 2026-09-13, review panel round 2 synthesis, folded. Phase 8's own success criteria still carried the unfalsifiable `rg -c 'tags local HEAD'` form across all three files, even though the Acceptance Criteria had already been split per file. The fix had landed in one place and not the other, which is the fold-lag class again, now on its fifth instance and its last.
- 2026-09-13, review panel round 3 (both seats, converging), folded. MUST-FIX of my own making: `-C` had no business in `mask_optarg`. Git overloads it three ways, `git -C <dir>` (global, a directory), `git commit -C <commit>` (reuse a message), `git switch -C <branch>` (force-create). Masking its value would have let `branch-name-guard.sh` miss the branch name in `git switch -C bad/branch` and ALLOW it, and would have hidden the target `git-no-dash-c.sh` exists to read. I copied the flag list off git-commit's options without checking the overload. `-C` is dropped: the measured false positive is `-m`, the long form `--reuse-message` covers the commit case unambiguously, and nothing measured needs `-C` masked. The per-gate table now also states that `git-no-dash-c.sh` applies NO optarg masking, because reading `-C` is its entire job.
- 2026-09-13, review panel round 3 (both seats), folded. The per-gate masker table only covered four gate classes while the phase test lists carried cases for five more. Rows added for the destructive-op gate, the commit-on-main gate, `branch-name-guard.sh`, `branch-pr-title-guard.sh` and `git-no-dash-c.sh`, so every tested gate has a stated masker set and every masker has a named test. `mask_dquote` had no named case in any phase and now has one.
- 2026-09-13, review panel round 3 (staff), folded: five stale phase cross-references left by the Phase 3 split renumbering (`:27`, `:464`, `:468`, `:471`, and the four-versus-six guard count at `:80`).
- 2026-09-13, recorded: the `git-release-guard.sh --self-test` matrix and the `$TMPDIR` body-file fixture were run by the author, not by a reviewer, because both need a writable fixture repo.
- 2026-09-13, review panel round 3 (both seats, independently, and both made it their hardest question), folded as spike 0e. M-1: after Phase 6 there are TWO hooks emitting `updatedInput` on one Bash event, `rewrite-cd-read.py` and the converted `git-no-dash-c.sh`. Nothing proves two rewrites compose, whether a later hook is fed the original or the rewritten input, or whether a later deny cancels an earlier rewrite. If both are handed the original payload and both emit a full replacement, last-writer-wins discards one silently, and the discarded one could be the branch that preserved `dangerouslyDisableSandbox`: the same scar as `rewrite-cd-read.py:920-926`, reached by a different route. Worth saying plainly, because it is mine: moving `git -C` off rails removed a three-rules-compose risk and introduced a two-rewrites-compose risk, and I did not notice the trade when I made it. Spike 0e settles it before Phase 6 is written.
- 2026-09-13, author, DECLINED the panel's recommendation to add M-1 to Open Questions. It is a verifiable fact about the harness, not a decision for Scott, and `taste.md` is explicit that open questions are the author's to close by probing rather than punting. It belongs in Phase 0, which is exactly the section for an unproven environmental assumption, and Open Questions stays holding only OQ1.
- 2026-09-13, review panel round 3, folded. M-2: the `git clean -f` gate at `git-release-guard.sh:326` reads `$untracked`, derived at `:99` from the same session-cwd `porcelain` as the destructive-op gate, so it carries the identical wrong-worktree bug. It is also the gate that deletes untracked files, which are the one class `reset --hard` cannot touch and `rkvr` exists for, so leaving it renamed-but-untested was the wrong place to be thin. Phase 4 now carries its cases.
- 2026-09-13, review panel round 3, folded. M-3: the risk table still asserted `flag_value` returns empty on an unexpanded `$`, contradicting the Data Model and the round-2 decision that the library always returns raw text.
- 2026-09-13, review panel round 3, recorded rather than folded, because it strengthens a change already in the plan. The effective-worktree fix does not merely correct a false verdict, it closes a live hole. The panel ran all three orientations against the guard: session dirty and target clean denies today and becomes allow (the op runs in a clean tree, nothing lost); session CLEAN and target dirty ALLOWS today, meaning a `git reset --hard` against 19 dirty entries is ungated right now; the untracked-file gate has the same orientation.
- 2026-09-13, review panel round 2 (architect; staff seat died at its banner and produced nothing this round), folded. Four findings, three of which broke something the round-1 fold had introduced:
  - Under-masking. Leaving quotes intact for option gates, which round 1 correctly forced, opens the mirror hole: `git tag -a v1 -m "added --force"` would trip the new tag-force gate on prose inside a message. Fixed with `mask_optarg`, which neutralizes the VALUE of `-m`, `--message`, `-F`, `--file`, `--reuse-message`, `--reedit-message`. Masking by quote character cannot separate `git push origin "--tags"` (a real option) from `git tag -m "--force"` (prose); masking by argument role can. This is the correct shape, not a patch.
  - `flag_value` returning empty on an unexpanded `$` fails CLOSED one layer up. Gate D would receive an empty `--body-file` path, find no `Release:` line, and deny, which is the exact `$TMPDIR` friction this chunk exists to remove. The library now always returns raw text and each caller decides: `branch-pr-title-guard.sh` passes through on an unknowable `--title`, `git-release-guard.sh` expands the four spellings it knows.
  - `cmdword_is git` has to apply to every git gate in the file, not only the `--tags` gate named in the table, or `echo "git tag -f v1"` trips the new gate.
  - AC1 was wrong as written: Phase 6 makes `git-no-dash-c.sh` source `lib.sh`, so the count is eight files, not seven. My own later change broke my own criterion, which is the second time this doc's gate has caught that class.
- 2026-09-13, review panel round 2 (architect), folded, and it added a phase. The `git-release-guard.sh` work splits into a parser swap that changes no verdicts and then the gate changes. A 54-case regression net cannot validate a refactor if the same commit edits the assertions: "the cases prove the swap changed nothing" is only true while the expected outcomes hold still. Ten phases now, 0 through 9, and the count is stated rather than smoothed over.
- 2026-09-13, review panel round 2 (architect), folded. Every phase wires its own `*-test.sh` into `.otto.yml` in the phase that introduces it, so `.otto.yml` is created in Phase 1 rather than Phase 8. A test that first reaches CI three phases later does not gate the commit that added the code it covers.
- 2026-09-13, review panel round 2 (architect), WITHDRAWN BY THE REVIEWER. Its round-1 headline must-fix, that `_PAT(?![A-Za-z])` is invalid regex, was retracted after it read `secret-echo-guard.sh:26` and confirmed the guard matches in python. The pushback was correct and the consensus loop closed it without escalation.
- 2026-09-13, review panel round 1 synthesis, folded. The panel scored its findings against the LIVE file rather than its dispatch snapshot and found that my own folding had left six sites describing the superseded design, two of them in the worst places: Phase 1 still told the implementer to lift `strip_heredocs` verbatim (which would have copied the 2a bypass into all five guards), and a Resolved Decision still said `git-no-dash-c.sh` is deleted when the plan converts it. All six corrected, plus two cheap-wins (a `git tag -fa` combined-flag case, and the Gate C negative case proving it matches the extracted branch name).
- 2026-09-13, review panel round 1, folded: the heredoc bypass reproduced end to end with a destructive command, not just a flag. On a 17-entry dirty tree `git reset --hard` denies, while `echo "<<EOF"` followed by `git reset --hard` ALLOWS. This is live on `main` today and is independent of this design; it is the strongest single reason the parser work is worth doing.
- 2026-09-13, review panel round 1 (architect + staff engineer), folded. Both seats independently raised the same must-fix and it inverted part of the design: masking double-quoted spans for OPTION detection is unsafe, because `git push origin "--tags"` passes `--tags` to git exactly as the bare form does. The draft would have allowed the bulk tag push the gate exists to stop. Corrected: option gates never mask quotes; what actually removes the four measured false-positive classes is `mask_heredoc`, nested-statement yielding, and the `cmdword_is git` anchor. (Round 2's synthesis later showed masking subshells was wrong for the same reason; see below.)
- 2026-09-13, review panel round 1, folded. `strip_heredocs` cannot be lifted verbatim: it is not quote-aware, so `echo "<<EOF"` opens a heredoc and every following line is consumed. Reproduced against the live function 2026-09-13 (a following `git push --tags` vanished). That is a deny BYPASS in the guard carrying the irreversible gates, so it is promoted to problem 2a rather than buried in Phase 1.
- 2026-09-13, review panel round 1 (architect), folded. Offsets must never cross back into bash: `grep -b -o` reports bytes, `${var:off:len}` counts characters, and they diverge on any multibyte input. Verified. The masking, matching and slicing now all happen inside one `awk` program behind `flag_value`, and a multibyte extraction case is in the Phase 1 criteria.
- 2026-09-13, review panel round 1 (architect), folded. `--body-file` expansion is literal string replacement of four spellings against the hook's own environment, never `eval`. An `eval` there would execute text that arrives inside a tool call.
- 2026-09-13, review panel round 1 (staff), folded. `secret-echo-guard.sh` must mask single-quoted spans and backslash-escaped `$`: bash expands nothing there, so those three forms are false positives. Double quotes stay unmasked, which is the same shell semantics read in the other direction. The exception is now stated as "follow bash's own expansion rules" rather than as a special case.
- 2026-09-13, review panel round 1 (staff), folded. `bin/hooks-resolve` takes `--settings` and `--root` (a fixture cannot be tested against a hard-coded path, and CI has no symlinked home), and shares ONE command-string parser with `hooks-preflight.sh`. The `bash '<path>' session` shape is specifically the one a first-token check passes without earning it.
- 2026-09-13, review panel round 1 (staff), folded. AC7 as first written was unfalsifiable for `release-driver.md`: `rg -c 'tags local HEAD'` matches the other two files and not that one, which says "`bump` (tags HEAD)". Split per file. This is exactly the defect class the ready-to-build gate exists to catch, found by running the criterion rather than reading it.
- 2026-09-13, review panel round 1 (both seats), folded. An emergency rollback is specified for the one new failure mode this chunk creates: six guards sourcing one `lib.sh`. Defensive sourcing degrades a BROKEN library to pass-through the same way a missing one does, `bash -n` in CI stops it merging, and `rm ~/.claude/hooks/lib.sh` is the operator kill switch.
- 2026-09-13, author, PUSHED BACK on architect must-fix "Phase 2 specifies an invalid regex for standard POSIX grep". The finding assumes `grep -E`. `secret-echo-guard.sh:26` pipes the command into a `python3` heredoc and matches with `re.search`, so a negative lookahead is valid and the reviewer's proof (`echo "GITHUB_PAT" | grep -E ...`) tests a program this repo does not run. Verified 2026-09-13 in python against all five names: `RIPGREP_CONFIG_PATH` no match, `MY_PATTERN` no match, `GITHUB_PAT` match, `GITHUB_PAT_HOME` match, `ANTHROPIC_API_KEY` match. The design stands; the doc now names the language at the point of the change so the next reader does not repeat the inference.
- 2026-09-13, author, PARTIALLY ACCEPTED architect finding "the `--tags` mask is an unsafe edge case" (`git push origin $(echo "--tags")`). The escape is real and is now recorded as a known limit in the Data Model: a subshell whose OUTPUT is a flag cannot be seen by any static matcher without executing it. It was not a reason to drop subshell handling, because the alternative leaves the measured `$(git describe --tags)` false positive in place. SUPERSEDED in shape by the round-2 synthesis below: subshell bodies are no longer masked at all, they are yielded as nested statements, which fixes the escape the architect named as well. The known limit that survives is narrower: a subshell whose OUTPUT is a flag, `git push origin $(echo "--tags")`, still cannot be seen, because the inner statement is `echo` and only running it would reveal the flag.
- 2026-09-13, author, ACCEPTED staff finding on `git restore` scope. `git restore --staged` and `--source=` discard the index as well as the worktree, which is strictly more loss than the path-scoped worktree revert the 66 denials were about. Those stay denied; the allowance covers plain `git checkout -- <paths>` and plain worktree `git restore <paths>` only.
- 2026-09-13, author: one sourced `hooks/lib.sh`, not a port of the bash guards onto the rails TypeScript parser. rails is early access and fails open, it cannot deny on the paths these guards must deny, and PreToolUse is the only layer subagents share (67% of the `git -C` fires were subagents). The heredoc stripper being lifted already works and is under test.
- 2026-09-13, author: a guard whose `lib.sh` is missing passes through with one stderr line rather than denying. A parser library that is absent must not wedge every Bash call; `hooks-preflight.sh` checks `lib.sh` by name, so the failure is loud at the next session start instead of silent.
- 2026-09-13, author: the `git -C` comparison is a string match against `$.session.cwd()`, not a resolved `realpath`. `git -C <cwd>` is harmless, so a missed strip costs nothing and leaves the model's own command intact. A resolved match would buy a handful of the 249 cases at the price of a filesystem dependency in a rewrite rule.
- 2026-09-13, author, SUPERSEDED the same day by the `updatedInput` finding below: the draft deleted `git-no-dash-c.sh` outright. The evidence for removing the DENY stands unchanged (249 denials, 235 with the target equal to cwd, zero cross-repo saves, every one reissued: there is no case the deny protects), but the file is kept and converted to a rewrite rather than deleted. Recorded rather than edited away, because the deny-has-no-value argument is what justifies the conversion.
- 2026-09-13, author, confirmed by both reviewers independently, scoped to the PLAIN sequence: `git stash && git reset --hard` is NOT a gap and nothing is built for it. Staff review named the variants that DO lose work (`git stash --patch`, `git stash --staged && git reset --hard`) because they leave tracked changes outside the stash; those stay denied by the existing dirty-tree check, which this chunk does not relax for `reset --hard`. In one command the hook reads `porcelain` before the stash runs, sees a dirty tree, and denies today. Split across two calls, the stash IS the backup: the tracked work is recoverable with `git stash pop`, and `reset --hard` does not touch untracked files. The audit listed it under "defeats the dirty check" (incidents proposal 12); that is true of the check and false of the data.
- 2026-09-13, author: the path-scoped revert is ALLOWED including a directory argument, not just a single file. The measured harm was the dodge, not the revert: 66 denials produced 12 `git show HEAD:f > f` workarounds that destroy the same bytes with no guard at all. An explicit path is the operator naming what to discard. Tree-wide forms (`.`, `./`, `:/`, `*`, no path) and `reset --hard` stay denied.
- 2026-09-13, author: `voice.md` is out of scope. It has no frontmatter, so it is already always-on and the rendered prompt is already correct. Chunk H owns the always-on prefix.
- 2026-09-13, author: this chunk documents the `Release:` line in `rules/pr.md` but does NOT build the auto-inject. The helper is chunk E's, per the program tracker, and folding it in early breaks the program rule.

