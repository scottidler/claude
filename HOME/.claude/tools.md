## My CLIs

<!-- GENERATED by ~/.claude/bin/gen-tools — do not edit by hand. -->
<!-- Re-run `~/.claude/bin/gen-tools` after installing a new custom binary. -->

Custom binaries in `~/.cargo/bin`, built from `~/repos`. Run `<tool> --help` for details.

- `ccu` — Fast CLI tool for tracking Claude Code session costs from JSONL logs
- `claude-permit` — Manage Claude Code permission hygiene - log, audit, suggest, report
- `cr` — Scan Claude Code session JSONL files and emit a per-host JSON report
- `cs` — RLM-style external context store
- `cxn` — A CLI tool for quick ping and DNS connectivity checks
- `github-setup-rs` — Manage GitHub organization repository configurations at scale
- `kat` — Concatenate files with metadata
- `kondo` — Organize files by moving them to the right directories based on extension
- `lint-unused` — Detect underscore-prefixed variable bindings that suppress unused warnings
- `loopr` — Agent orchestrator; operates on a target repo via decompose/implement/integrate.
- `manifest` — A tool for managing system configurations and installations via YAML manifests
- `mermaid-rs` — A Rust CLI tool for generating Mermaid diagrams programmatically
- `nerf` — Tone down volatile comments and messages with help from GPT
- `openshotx` — OpenShotX - Screenshot tool for Linux
- `pd` — PagerDuty incident management CLI
- `pyr` — Fast Python codebase analysis for agentic LLMs
- `qai` — Natural language to shell commands via LLM
- `readtime` — Estimate reading time for text documents
- `repo` — A Git workflow simulation tool
- `requote` — tool for requoting double->single or single->double
- `sb` — second-brain unified CLI: borg ingestion + cortex governance + oracle MCP
- `scan` — A comprehensive network scanner
- `scrabbler` — Simple program to spell words in Scrabble format
- `signal-rs` — Native-Rust Signal client - library + CLI; v0.1 unblocks borg Note-to-Self ingest
- `slam` — HPA: horizontal PR autoscaler
- `taskstore` — Generic persistent state management with SQLite, JSONL, and Git
- `td` — Ralph Wiggum loop orchestrator for concurrent AI workflows
- `tmp` — Helper function to quickly make file types defined in the config file
- `viewport` — Draggable screen region capture tool that outputs to a virtual camera
- `viewport2` — Draggable screen region capture tool that outputs to a virtual camera
- `worktree` — Switch between or create git worktrees in a bare container (use this, never raw `git worktree add`)
- `workweek` — Calculate the work week containing a given date
- `yl` — A YAML linter written in Rust
- `ytx` — YouTube transcript extractor
