# Anthropic runtime (managed by Claude Code itself)
history.jsonl
mcp-needs-auth-cache.json
cache/
debug/
file-history/
paste-cache/
plans/
plugins/
projects/
session-env/
shell-snapshots/
stats-cache.json
statsig/
tasks/
teams/
telemetry/
todos/
downloads/
backups/
image-cache/
policy-limits.json
usage-data/

# Machine-specific (MCP server configs, tokens)
settings.local.json
settings.local.json.orig
settings.json.orig

# Statusline config
powerline/

# Auto memory (project-scoped, generated per-machine)
memory/

# Third-party skills managed by the `skills` CLI (https://github.com/vercel-labs/skills),
# not manifest-managed here. Reinstall: npx skills add <owner/repo> -g --skill <name> --agent claude-code -y
HOME/.claude/skills/archify/

# Credentials and sessions (per-machine, sensitive)
.credentials.json
sessions/
